import { SplitDirection } from "../types";

// export const useCanSplitWidgetInLayout = (layout: string, idx: number, direction: SplitDirection): boolean => {
//     const layout = useCelera((state) => state.layouts[layout]);
// }
